services:
  app:
    build:
      context: .
      dockerfile: docker/app.Dockerfile
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://quiz:quiz@10.0.0.4:5432/quizdb
      REDIS_URL: redis://10.0.0.5:6379
      ADMIN_JWT_SECRET: Borusannext2026
      ADMIN_USER: admin
      ADMIN_PASS: borusannextyarisma2026
    restart: unless-stopped
    expose:
      - "3000"

  nginx:
    image: nginx:1.27-alpine
    depends_on:
      - app
    ports:
      - "80:80"
    volumes:
      - ./infra/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./web/dist:/usr/share/nginx/html:ro
    restart: unless-stopped
